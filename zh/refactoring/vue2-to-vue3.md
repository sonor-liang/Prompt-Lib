# Vue2 → Vue3 重构

请将以下 Vue2 组件重构为 Vue3 Composition API，要求：

## 重构目标

- 替换 Options API 为 Composition API
- 添加 TS 类型定义
- 移除 Vue2 特有 API（如 $emit/$refs 的旧用法）
- 优化性能（减少不必要的响应式数据）

## 原代码

```vue
{{Vue2组件代码}}
```

## 输出要求

- 重构后的完整 .vue 文件
- 重构点说明（逐条列出）
- 重构后的测试注意事项
