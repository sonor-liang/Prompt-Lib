# API Request Function Generation

Please encapsulate a frontend request function based on Axios, tech stack: TS + Axios, requirements:

## Function Requirements

- Request interceptor: add token, unified request headers
- Response interceptor: unified error handling, format response data
- Cancel duplicate requests: prevent duplicate requests for same endpoint
- Timeout handling: default 30s, customizable

## Output Requirements

Complete request.ts file + usage examples + error code documentation
